#define REP(i,n) for(int i = 0; i < (n); ++i)
#define VAR(x,a) __typeof(a) x(a)
#define FOREACH(it,v) for(VAR(it,(v).begin());it!=(v).end();++it)

#define ROUND_TO_INT(r) (int)((r > 0.0) ? (r + 0.5) : (r - 0.5))

